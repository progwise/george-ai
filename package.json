{
  "name": "george-ai",
  "version": "1.0.0",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "yarn@3.6.1",
  "scripts": {
    "codegen:pothosGraphql": "graphql-codegen --config graphql.config.ts --project pothosGraphql",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "check-format": "prettier --check .",
    "codegen": "node codegen.ts"
  },
  "devDependencies": {
    "@graphql-codegen/cli": "^5.0.0",
    "@graphql-codegen/client-preset": "^4.1.0",
    "prettier": "^3.0.1",
    "turbo": "latest"
  }
}
