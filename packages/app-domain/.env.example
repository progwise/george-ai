DATABASE_URL="postgresql://chatweb:password@gai-chatweb-db:5432/chatweb?schema=public"
CRAWLER_CREDENTIALS_DIR=./.crawler-credentials

# Optional: Override test database connection (defaults to devcontainer values)
# TEST_DB_HOST=gai-chatweb-db-test
# TEST_DB_PORT=5432
# TEST_DB_USER=chatweb-test
# TEST_DB_PASSWORD=password
# Note: Database name is always 'app-domain-test' (configured in src/testing/global-setup.ts)
