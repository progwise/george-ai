### Pothos GraphQL Backend

Set up the environment variables:

Create a `.env` file for the `DATABASE_URL` similar to the `.env.example`.

### GraphQL playground

Access the GraphQL playground at `http://localhost:3003/`.
