# ===================================================================
# George AI - E2E Tests - Local Development Configuration
# ===================================================================
# For running E2E tests against your local devcontainer environment
#
# Prerequisites:
#   1. VS Code with devcontainer running
#   2. pnpm dev running (webapp + backend)
#   3. Test user created in local Keycloak (http://localhost:8180)
#
# Note: Keycloak configuration is NOT needed here - the webapp/backend
#       already have Keycloak configured in their own .env files.
#       E2E tests just hit localhost:3001 and use those configs.
# ===================================================================

# -------------------------------------------------------------------
# Database Connection (REQUIRED)
# -------------------------------------------------------------------
# Used by test setup to create test workspaces via SQL
DATABASE_URL=postgresql://chatweb:chatweb@localhost:5434/chatweb

# -------------------------------------------------------------------
# E2E Test User Credentials (REQUIRED)
# -------------------------------------------------------------------
# Create this user in your local Keycloak: http://localhost:8180
# Realm: george-ai (default)
E2E_USERNAME=test
E2E_EMAIL=playwright@test.de
E2E_PASSWORD=Test123!

# ===================================================================
# CI-ONLY Variables (not needed for local development)
# ===================================================================
# The docker-compose.yml in this directory is ONLY used by CI
# (GitHub Actions), not by local developers. It has its own
# Keycloak configuration that CI uses.
#
# Local developers: ignore these, they're set by CI
# ===================================================================
# GIT_COMMIT_SHA=e2e-test
# EXPECTED_DISCORD_URL=https://discord.gg/GbQFKb2MNJ
