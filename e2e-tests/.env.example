# George AI - E2E Tests Environment Configuration
# Copy this file to .env and adjust values as needed

# Git commit SHA (optional, defaults to 'e2e-test')
GIT_COMMIT_SHA=e2e-test

# Keycloak (shared production instance with test realm)
# Uses keycloak.george-ai.net by default - no local Keycloak needed!
KEYCLOAK_URL=https://keycloak.george-ai.net
KEYCLOAK_REALM=george-ai-e2e-test
KEYCLOAK_CLIENT_ID=george-ai-e2e

# E2E Test User Credentials
# For local testing: Get credentials from team maintainer or README setup section
# For CI: Stored in GitHub Secrets (E2E_USERNAME, E2E_PASSWORD)
E2E_USERNAME=
E2E_PASSWORD=

# Ollama (optional - shared remote instance)
# Leave empty to skip AI-dependent tests
OLLAMA_BASE_URL=
OLLAMA_API_KEY=
