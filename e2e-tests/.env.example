# ===================================================================
# George AI - E2E Tests - Local Development Configuration
# ===================================================================
# For running E2E tests against your local devcontainer environment
#
# Prerequisites:
#   1. VS Code with devcontainer running
#   2. pnpm dev running (webapp + backend)
#   3. Test user created in local Keycloak (http://localhost:8180)
#
# Note: Keycloak configuration is NOT needed here - the webapp/backend
#       already have Keycloak configured in their own .env files.
#       E2E tests just hit localhost:3001 and use those configs.
# ===================================================================

# -------------------------------------------------------------------
# Base URL (OPTIONAL)
# -------------------------------------------------------------------
# URL where the webapp is running. Defaults to http://localhost:3001
# Uncomment and modify if testing against different host/port:
# BASE_URL=http://localhost:3001
#
# Examples:
# - Testing host Docker from devcontainer: BASE_URL=http://host.docker.internal:3001
# - Testing remote instance: BASE_URL=http://192.168.1.100:3001
# - Testing production: BASE_URL=https://app.george-ai.net

# -------------------------------------------------------------------
# Marketing Website URL (OPTIONAL)
# -------------------------------------------------------------------
# URL where the marketing website (georgeai-web) is running. Defaults to http://localhost:4321
# For docker-compose.local.yml (which uses port 4331 to avoid conflicts):
MARKETING_WEBSITE_URL=http://localhost:4331
#
# Examples:
# - Local pnpm dev: MARKETING_WEBSITE_URL=http://localhost:4321 (default)
# - docker-compose.local: MARKETING_WEBSITE_URL=http://localhost:4331
# - CI: Uses default port 4321

# -------------------------------------------------------------------
# Database Connection (REQUIRED)
# -------------------------------------------------------------------
# Used by test setup to create test workspaces via SQL
DATABASE_URL=postgresql://chatweb:chatweb@localhost:5434/chatweb

# Encryption key for sensitive connector credentials (required for automations)
# Must be 64 hex characters (32 bytes). Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# -------------------------------------------------------------------
# E2E Test User Credentials (REQUIRED)
# -------------------------------------------------------------------
# Create this user in your local Keycloak: http://localhost:8180
# Realm: george-ai (default)
E2E_USERNAME=test
E2E_EMAIL=playwright@test.de
E2E_PASSWORD=Test123!

# ===================================================================
# CI-ONLY Variables (not needed for local development)
# ===================================================================
# The docker-compose.yml in this directory is ONLY used by CI
# (GitHub Actions), not by local developers. It has its own
# Keycloak configuration that CI uses.
#
# Local developers: ignore these, they're set by CI
# ===================================================================
# GIT_COMMIT_SHA=e2e-test
# EXPECTED_DISCORD_URL=https://discord.gg/GbQFKb2MNJ


OPENAI_API_KEY=

# Primary OLLAMA instance (remote 0.9.0)
OLLAMA_BASE_URL=
OLLAMA_API_KEY=
OLLAMA_VRAM_GB=16

# -------------------------------------------------------------------
# SMB Test Server (OPTIONAL - for SMB crawler tests)
# -------------------------------------------------------------------
# Hostname of the SMB test server for E2E crawler tests
# Defaults to 'gai-smb-test' (devcontainer environment)
# E2E_SMB_TEST_SERVER=gai-smb-test
#
# Examples:
# - Local devcontainer: E2E_SMB_TEST_SERVER=gai-smb-test
# - CI environment: E2E_SMB_TEST_SERVER=gai-e2e-smb-test
# - docker-compose.local: E2E_SMB_TEST_SERVER=gai-e2e-smb-test