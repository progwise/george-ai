name: Deploy to 1blu

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4
      - name: Build the Docker image
        run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)

  deploy:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4
      - name: Deploy to 1blu
        run: |
          echo "Deploying to 1blu..."
          # scp -r build/* ${{ secrets.SSH_USER }}@ssh.strato.de:/kunden/homepages/1/d123456789/htdocs/
          echo "Deployed to 1blu"
