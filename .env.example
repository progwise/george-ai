# George AI - Development Environment Configuration
# This file is for local development with devcontainer (pnpm dev)
#
# For production Docker Compose deployments, use:
#   docs/examples/.env.example instead
#
# Setup instructions: docs/developer-setup.md

# Logging Configuration
# LOG_LEVEL: Controls logging verbosity (ERROR, WARN, INFO, DEBUG)
# Default: INFO
# Use DEBUG for detailed troubleshooting, ERROR for production
LOG_LEVEL=INFO

TYPESENSE_API_HOST=gai-typesense
TYPESENSE_API_PORT=8108
TYPESENSE_API_PROTOCOL=http
TYPESENSE_API_KEY=xyz

# Database Connection
# Connection pool settings (recommended for production):
#   connection_limit: Max connections in pool (default: num_cpus * 2 + 1)
#   pool_timeout: Seconds to wait for connection (default: 10)
# Example with pool settings: postgresql://user:pass@host:5432/db?schema=public&connection_limit=20&pool_timeout=30
DATABASE_URL="postgresql://chatweb:XXX@gai-chatweb-db:5432/chatweb?schema=public"

# Encryption key for sensitive connector credentials (required for automations)
# Must be 64 hex characters (32 bytes). Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

GRAPHQL_API_KEY=TestKey
BACKEND_URL=http://localhost:3003
BACKEND_PUBLIC_URL=http://localhost:3003

GOOGLE_DRIVE_CLIENT_ID=
GOOGLE_DRIVE_CLIENT_SECRET=
# How to get Google Drive credentials:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Google Drive API
# 4. Create OAuth 2.0 credentials (Web application)
# 5. Add authorized redirect URIs for your application
# Full guide: https://developers.google.com/drive/api/guides/enable-sdk

UPLOADS_PATH=./apps/georgeai-backend/.uploads
CRAWLER_CREDENTIALS_DIR=./.crawler-credentials

KEYCLOAK_REDIRECT_URL=http://localhost:3001
KEYCLOAK_REALM=george-ai
KEYCLOAK_URL=http://localhost:8180
KEYCLOAK_CLIENT_ID=george-ai-web

PUBLIC_APP_URL=http://localhost:3001

GIT_COMMIT_SHA=local-dev

# SMTP Email Configuration (optional - for notifications and contact forms)
# SMTP_HOSTNAME=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-username
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM=noreply@george-ai.net
# SMTP_TEST_RECIPIENT=your-email@example.com  # Required for mailer integration tests

OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_API_KEY=

# OLLAMA VRAM Configuration (GB) - defaults to 16GB if not set
OLLAMA_VRAM_GB=32

# Multiple OLLAMA instances (optional)
OLLAMA_BASE_URL_1=http://host.docker.internal:11434
OLLAMA_API_KEY_1=
OLLAMA_VRAM_GB_1=64

OLLAMA_BASE_URL_2=https://ollama.example.com:11434
OLLAMA_API_KEY_2=your-api-key-here
OLLAMA_VRAM_GB_2=128

# OpenAI Configuration (optional - all providers are optional)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Optional: Use custom base URL for Azure OpenAI or compatible endpoints
# OPENAI_BASE_URL=https://api.openai.com/v1

# Auto Start Workers
AUTOSTART_ENRICHMENT_WORKER=false
AUTOSTART_CONTENT_PROCESSING_WORKER=false