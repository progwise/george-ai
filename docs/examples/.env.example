# George AI - Quick Start
# Copy to .env, set HOST_NAME, then: docker compose up -d
# Access via https://${HOST_NAME}

HOST_NAME=george-ai.local
DB_PASSWORD=george-ai-2024
KEYCLOAK_ADMIN_PASSWORD=admin
TYPESENSE_API_KEY=george-ai-typesense

# Encryption key for connector credentials (required for Automations)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# SMTP Email Configuration (optional - for notifications and invitations)
# SMTP_HOSTNAME=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-username
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM=noreply@george-ai.net
