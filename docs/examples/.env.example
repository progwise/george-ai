# George AI - Production Environment Configuration
# This file is for production Docker Compose deployments
#
# Copy this file and customize for your deployment:
#   cp docs/examples/.env.example .env
#
# For local development (pnpm dev), use the root .env.example instead
# Setup guide: docs/self-hosting.md

# ============================================
# Database Configuration
# ============================================

# Main PostgreSQL Database
DB_USER=george
DB_PASSWORD=changeme_secure_password
DB_NAME=georgeai
DB_PORT=5432

# ============================================
# Keycloak Authentication
# ============================================

# Keycloak Database
KEYCLOAK_DB_PASSWORD=changeme_keycloak_db_password

# Keycloak Admin Account
KEYCLOAK_ADMIN_PASSWORD=changeme_admin_password

# Keycloak Configuration
KEYCLOAK_PORT=8180
KEYCLOAK_REALM=george-ai
KEYCLOAK_CLIENT_ID=george-ai-client

# Keycloak URLs (adjust for your domain)
# For localhost: http://localhost:8180
# For production: https://keycloak.yourdomain.com
KEYCLOAK_URL=http://localhost:8180
KEYCLOAK_REDIRECT_URL=http://localhost:3001

# ============================================
# Typesense Vector Search
# ============================================

TYPESENSE_API_KEY=changeme_typesense_api_key
TYPESENSE_PORT=8108

# ============================================
# Backend Configuration
# ============================================

BACKEND_PORT=3003

# Public URL for backend (used by frontend)
# For localhost: http://localhost:3003
# For production: https://api.yourdomain.com
BACKEND_PUBLIC_URL=http://localhost:3003

# ============================================
# Frontend Configuration
# ============================================

FRONTEND_PORT=3001

# Public URL for frontend application
# For localhost: http://localhost:3001
# For production: https://yourdomain.com
PUBLIC_APP_URL=http://localhost:3001

# ============================================
# Ollama (Optional - Local AI)
# ============================================

# Ollama service URL
# Default connects to Ollama container: http://ollama:11434
# For external Ollama: http://your-ollama-host:11434
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_PORT=11434

# ============================================
# Optional Integrations
# ============================================

# SMTP Email Configuration (optional - for notifications)
# SMTP_HOSTNAME=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-username
# SMTP_PASSWORD=your-smtp-password

# Google Drive Integration (optional)
# GOOGLE_DRIVE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_DRIVE_CLIENT_SECRET=your-client-secret

# ============================================
# Production Notes
# ============================================
#
# 1. Change all passwords to secure random values
# 2. Update URLs to match your domain (no trailing slashes)
# 3. For HTTPS, use a reverse proxy (Nginx, Caddy, Traefik)
# 4. Keep this file secure - never commit with real credentials
# 5. For Docker Swarm, use Docker secrets instead of env vars
#
# See docs/self-hosting.md for complete deployment guide
