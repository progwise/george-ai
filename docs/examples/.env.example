# George AI - Quick Start
# Copy to .env and set HOST_NAME, then: docker compose up -d

HOST_NAME=localhost
DB_PASSWORD=george-ai-2024
KEYCLOAK_ADMIN_PASSWORD=admin
TYPESENSE_API_KEY=george-ai-typesense
